#!/usr/bin/env bash
# audit-readonly.sh — Placeholder for read-only audit scripts.
# This script is intended to be extended with domain-specific audit checks
# (security, performance, db-integrity, etc.) that run in read-only mode.
set -euo pipefail

echo "=== Audit (read-only) ==="
echo "No audit checks configured yet."
echo "Add domain-specific checks to this script as needed."
